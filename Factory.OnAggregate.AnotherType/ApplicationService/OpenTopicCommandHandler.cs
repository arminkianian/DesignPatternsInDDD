namespace Factory.OnAggregate.AnotherType.ApplicationService
{
    internal class OpenTopicCommandHandler
    {
        public void Handle()
        {
            // var forum=_forumRepository.Get(forumId);
            // var topic= forum.OpenTopic(title, ...);
            // _topicRepository.Add(topic);
        }
    }
}
